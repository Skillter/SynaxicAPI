# Synaxic API - Environment Variables Example
# Copy this file to .env and fill in the values before running the setup script.
# The setup script will generate random passwords for any that are left blank.
# DO NOT commit the actual .env file to your repository.

# --- Google OAuth2 Credentials ---
# Required for the "Sign in with Google" functionality.
# Obtain these from the Google Cloud Console.
GOOGLE_CLIENT_ID=your-google-client-id-goes-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-goes-here

# --- Main Production Database (PostgreSQL) ---
POSTGRES_DB=synaxic
POSTGRES_USER=synaxic
POSTGRES_PASSWORD=

# --- Main Production Cache (Redis) ---
REDIS_PASSWORD=

# --- Grafana Admin Credentials ---
GRAFANA_ADMIN_PASSWORD=

# --- Docker User Permissions ---
# These are used to run containers with the current user's permissions.
# Leave them empty and setup-main-vps.sh will set them automatically
DOCKER_UID=
DOCKER_GID=